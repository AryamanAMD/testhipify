diff a/src/samples/Samples/0_Introduction/simpleSurfaceWrite/simpleSurfaceWrite.cu.hip b/src/samples/Samples/0_Introduction/simpleSurfaceWrite/simpleSurfaceWrite.cu.hip	(rejected hunks)
@@ -296,8 +296,3 @@ void runTest(int argc, char **argv) {
   free(imagePath);
   free(refPath);
 }
-e(dData));
-  checkCudaErrors(hipFreeArray(cuArray));
-  free(imagePath);
-  free(refPath);
-}
